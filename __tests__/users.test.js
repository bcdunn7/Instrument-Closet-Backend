'use strict';

import app from '../app';
import db from '../db';
import User from '../models/user';

const request = require('supertest');

describe('GET /users', () => {
    it('gets all users: admin', async () => {

    })
})
